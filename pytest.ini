[pytest]
pythonpath = .
asyncio_mode = auto

addopts =
    --cov=auth_service
    --cov-report=term-missing

markers =
    unit: unit tests (fast, isolated)
    integration: integration tests (db, api)
    auth: auth_service tests
